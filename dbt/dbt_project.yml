name: on_prem_medallion
version: "1.0.0"
config-version: 2

profile: on_prem_medallion

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  on_prem_medallion:
    silver:
      +schema: silver
      +materialized: view
    gold:
      +schema: gold
      +materialized: table
